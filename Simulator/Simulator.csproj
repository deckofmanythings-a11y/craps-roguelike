<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Same pattern as RulesEngineTests: compile the plain rules-engine files
         from RulesEngine/. Balance-tuning tool (Monte Carlo bullet/round
         simulation), separate from RulesEngineTests' correctness assertions. -->
    <Compile Include="..\RulesEngine\*.cs" />
  </ItemGroup>

</Project>
